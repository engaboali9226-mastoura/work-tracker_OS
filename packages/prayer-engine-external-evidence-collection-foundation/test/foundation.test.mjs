import assert from "node:assert/strict";import {SOURCE_CLASSES,COLLECTION_PHASES,ACCEPTANCE_GATES,FAILURE_CODES,Lifecycle,CollectionError,authorization,assertFoundation,assertCollection,validateIntent,validateTarget,controls,assertControls,validateCapture,validateClaim,validateGate,canonical,validateManifest,validateSeal} from "../dist/index.js";const h="a".repeat(64),a=authorization();assertFoundation(a);assert.equal(SOURCE_CLASSES.length,6);assert.equal(COLLECTION_PHASES.length,12);assert.equal(ACCEPTANCE_GATES.length,12);assert.equal(FAILURE_CODES.length,18);assert.throws(()=>assertCollection(a),e=>e instanceof CollectionError&&e.code==="NETWORK_ACCESS_DENIED");const intent={runId:"evidence-0001",obligationId:"ES-001",mechanismId:"ML-ISO-01",sourceClass:"SRC-OFFICIAL",approvedHost:"docs.example.test",requirementsHash:h,designHash:h,landscapeHash:h,createdAt:"2026-07-26T07:40:00+03:00"};validateIntent(intent);let i=0,t=Array.from({length:12},(_,n)=>`2026-07-26T07:${40+n}:00+03:00`),l=new Lifecycle(intent.runId,()=>t[i++]??t.at(-1));for(const p of COLLECTION_PHASES.slice(1))l.advance(p);assert.equal(l.snapshot().terminal,true);const target=validateTarget("https://docs.example.test/x",["docs.example.test"]);assert.equal(target.host,"docs.example.test");assert.throws(()=>validateTarget("http://docs.example.test/x",["docs.example.test"]),e=>e instanceof CollectionError);const c={id:"capture-1",contentType:"text/html",bytes:10,sha256:h,retrievedAt:"2026-07-26T08:00:00+03:00",rawPreserved:true};validateCapture(c,100,["text/html"]);const claim={id:"claim-1",captureId:"capture-1",kind:"paraphrase",text:"Synthetic claim",location:"line:1",confidence:"HIGH"};validateClaim(claim);const gate={gate:"EG-01_AUTHORIZATION_AND_SCOPE",state:"NOT_READY",evidenceIds:[],blockers:["Collection unauthorized"]};validateGate(gate);const ct=controls();assert.throws(()=>assertControls(ct),e=>e instanceof CollectionError&&e.code==="COLLECTION_CONTROLS_UNSELECTED");const m={schema:"1",intent,lifecycle:l.snapshot(),controls:ct,captures:[c],claims:[claim],gates:[gate],networkUsed:false,externalCollection:false,screenshot:false,pdf:false,verification:false,experiment:false,mechanismRecommendation:false,mechanismSelection:false,mechanismImplementation:false,dependencyInstallation:false,candidateExecution:false,adapter:false,repositoryMutation:false,commit:false,tag:false,push:false};validateManifest(m);validateSeal(canonical(m),h);assert.throws(()=>canonical({recommendation:"x"}),e=>e instanceof CollectionError);console.log("NOOR_STEP_023_EXTERNAL_EVIDENCE_FOUNDATION_TESTS_PASS");